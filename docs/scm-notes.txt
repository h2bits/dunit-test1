- Should I commit binaries from the tools? (e.g. DCU's from the DUnit Framework)
- Should I commit the project's .cfg and .res files generated by the Delphi IDE? I think so.
- Learn more about the .dof file and check if it needs to be tracked.

